let chartSettings = {
    height: 250,
    type: 'rangeBar'
};

let plotOptionsSettings = {
    bar: {
        horizontal: true,
        barHeight: '70%',
        rangeBarGroupRows: true
    }
};

let colorsSettings = ["#00E396", "#775DD0", "#008FFB"];

let strokeSettings = {
    width: 1
};

let fillSettings = {
    type: 'solid',
    opacity: 0.7
};

let gridSettings = {
    show: true,
    borderColor: '#ededed',
    strokeDashArray: 0,
    position: 'back',
    xaxis: {
        lines: {
            show: true
        }
    }
};

let xAxisSettings = {
    type: 'datetime'
};

let legendSettings = {
    position: 'bottom'
};

let chartId = 1;
